apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configMap.name | default (include "common.fullname" .) }}
data:
{{- range $key, $val := .Values.configMap.data }}
    {{ $key }}: {{ $val | quote}}
{{ end -}}