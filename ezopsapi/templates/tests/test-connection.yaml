apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ezopsapi.fullname" . }}-test-connection"
  labels:
    {{- include "ezopsapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ezopsapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
